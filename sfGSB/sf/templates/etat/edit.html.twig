{% extends 'base.html.twig' %}

{% block titre %}Modifier l'état{% endblock %}

{% block body %}
    <!-- ======= Hero Section ======= -->
  <section id="hero" class="d-flex align-items-center">    
      <div class="container" data-aos="fade-up">
          <center>
          <div class="section-title">              
          <h2>Modifier l'état</h2>
        </div>    

            {{ include('etat/_form.html.twig', {'button_label': 'Modifier l\'état'}) }}
            <br/>
            <a href="{{ path('etat_index') }}" class="btn-menu animated fadeInUp scrollto">Revenir voir la liste</a>

            {{ include('etat/_delete_form.html.twig') }}
          </center>
      </div>
  </section>
{% endblock %}
