{% extends 'base.html.twig' %}

{% block titre %}Modifier la Fiche frais{% endblock %}

{% block body %}
     <section id="hero" class="d-flex align-items-center">    
      <div class="container" data-aos="fade-up">
          <center>
          <div class="section-title">              
          <h2>Modifier la Fiche Frais</h2>
        </div>
    

    {{ include('fichefrais/_form.html.twig', {'button_label': 'Modifier'}) }}

    <br/>
    <a href="{{ path('fichefrais_index') }}" class="btn-menu animated fadeInUp scrollto">Revenir voir la liste</a>

    {{ include('fichefrais/_delete_form.html.twig') }}
    
          </center>
      </div>
     </section>
{% endblock %}
